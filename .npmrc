# Evita fallos en CI/Cloud Build cuando pnpm rechaza deps
# publicadas hace <24h (ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION).
# Las versiones críticas se fijan además en package.json → pnpm.overrides.
minimum-release-age=0
